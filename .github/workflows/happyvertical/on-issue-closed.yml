# =============================================================================
# GENERATED FILE - DO NOT EDIT DIRECTLY
# =============================================================================
# This file is automatically generated by the HappyVertical standardization
# script. Any manual changes will be overwritten on the next update.
#
# Source: https://github.com/happyvertical/sdk/.github/workflows/on-issue-closed-template.yml
#
# To update:
#   bun scripts/standardize-repo.ts --repo [owner/repo] --path [path]
# =============================================================================

name: On Issue Closed

on:
  issues:
    types: [closed]

permissions:
  issues: write
  contents: read

jobs:
  cleanup:
    uses: happyvertical/sdk/.github/workflows/shared-issue-closer.yml@main
    with:
      issue_number: ${{ github.event.issue.number }}
      repo_owner: ${{ github.repository_owner }}
      repo_name: ${{ github.event.repository.name }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
