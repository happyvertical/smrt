/**
 * Subpath entry: `@happyvertical/smrt-sales/crm`.
 *
 * Leads, Opportunities, configurable Pipelines, sales activity, ownership,
 * and idempotent conversion links — without referral or commission behavior
 * in the CRM state machine.
 */

import './__smrt-register__.js';

export * from './crm/index.js';
