// Auto-generated by @smrt/core vite plugin
// DO NOT EDIT - changes will be overwritten

import { json } from '@sveltejs/kit';
import { getCollection } from '$lib/server/smrt';
import type { RequestHandler } from './$types';
// Note: @happyvertical/smrt-content:ContentCorrection is auto-registered by the Vite plugin scanner

// List all @happyvertical/smrt-content:contentcorrections
export const GET: RequestHandler = async ({ url }) => {
  const limit = Number(url.searchParams.get('limit')) || 50;
  const offset = Number(url.searchParams.get('offset')) || 0;

  const collection = await getCollection(
    '@happyvertical/smrt-content:ContentCorrection',
  );
  const items = await collection.list({ limit, offset });
  const count = await collection.count();

  return json({ items, count, limit, offset });
};

// Create new @happyvertical/smrt-content:contentcorrection
export const POST: RequestHandler = async ({ request }) => {
  const data = await request.json();

  const collection = await getCollection(
    '@happyvertical/smrt-content:ContentCorrection',
  );
  const item = await collection.create(data);
  await item.save();

  return json(item, { status: 201 });
};
