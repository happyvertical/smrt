# =============================================================================
# GENERATED FILE - DO NOT EDIT DIRECTLY
# =============================================================================
# This file is automatically generated by the HappyVertical standardization
# script. Any manual changes will be overwritten on the next update.
#
# Source: https://github.com/happyvertical/sdk/.github/workflows/on-pr-opened-template.yml
#
# To update:
#   bun scripts/standardize-repo.ts --repo [owner/repo] --path [path]
# =============================================================================

name: On PR Opened

on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  handle-pr:
    uses: happyvertical/sdk/.github/workflows/shared-pr-handler.yml@main
    with:
      pr_number: ${{ github.event.pull_request.number }}
      pr_title: ${{ github.event.pull_request.title }}
      pr_body: ${{ github.event.pull_request.body }}
      repo_owner: ${{ github.repository_owner }}
      repo_name: ${{ github.event.repository.name }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
