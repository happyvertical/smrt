# Function: demonstrateClient()

> **demonstrateClient**(): `Promise`\<`void`\>

Defined in: [packages/products/src/client.ts:11](https://github.com/happyvertical/smrt/blob/bfd2feaea84273ee833a92e2d20c959aedfcfbd9/packages/products/src/client.ts#L11)

SMRT Template: Triple-Purpose Export

This is the main entry point when this template is used as an NPM library.
It re-exports everything from the lib directory for easy consumption.

Usage examples:

// Import models
import { Product, Category } from '@happyvertical/smrt-core-template';

// Import UI components
import { ProductCard, ProductForm } from '@happyvertical/smrt-core-template/components';

// Import stores
import { ProductStoreClass } from '@happyvertical/smrt-core-template/stores';

## Returns

`Promise`\<`void`\>
