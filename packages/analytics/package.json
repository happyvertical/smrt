{
  "name": "@happyvertical/smrt-analytics",
  "version": "0.19.59",
  "description": "Analytics integration models for SMRT framework",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./manifest": "./dist/manifest.json",
    "./manifest.json": "./dist/manifest.json"
  },
  "scripts": {
    "build": "vite build --mode library && node ../../scripts/generate-stub-from-manifest.js",
    "build:watch": "vite build --mode library --watch",
    "clean": "rm -rf dist",
    "dev": "vite dev",
    "generate:test": "node scripts/generate-test-manifest.js",
    "test": "pnpm run generate:test && npx vitest run --passWithNoTests",
    "test:sqlite": "TEST_DB_ADAPTER=sqlite pnpm run generate:test && npx vitest run --project sqlite --passWithNoTests",
    "test:postgres": "TEST_DB_ADAPTER=postgres pnpm run generate:test && npx vitest run --project postgres --passWithNoTests",
    "test:json": "TEST_DB_ADAPTER=json pnpm run generate:test && npx vitest run --project json --passWithNoTests",
    "test:watch": "npx vitest"
  },
  "dependencies": {
    "@happyvertical/smrt-core": "workspace:*"
  },
  "devDependencies": {
    "@happyvertical/smrt-vitest": "workspace:*",
    "@happyvertical/sql": "^0.66.9",
    "@types/node": "25.0.9",
    "fast-glob": "3.3.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.17"
  },
  "keywords": [
    "ai",
    "smrt",
    "analytics",
    "google-analytics",
    "plausible",
    "tracking"
  ],
  "author": "HAVE Team",
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}
