// Auto-generated by @smrt/core vite plugin
// DO NOT EDIT - changes will be overwritten

import { json, error } from '@sveltejs/kit';
import type { RequestHandler } from './$types';
import { getCollection } from '$lib/server/smrt';

// Get single contentcontributor
export const GET: RequestHandler = async ({ params }) => {
  const collection = await getCollection<any>('@happyvertical/smrt-content:ContentContributor');
  const item = await collection.get(params.id);
  if (!item) throw error(404, '@happyvertical/smrt-content:ContentContributor not found');

  return json(item);
};

// Update contentcontributor
export const PUT: RequestHandler = async ({ params, request }) => {
  const collection = await getCollection<any>('@happyvertical/smrt-content:ContentContributor');
  const item = await collection.get(params.id);
  if (!item) throw error(404, '@happyvertical/smrt-content:ContentContributor not found');

  const data = await request.json();
  Object.assign(item, data);
  await item.save();

  return json(item);
};

// Delete contentcontributor
export const DELETE: RequestHandler = async ({ params }) => {
  const collection = await getCollection<any>('@happyvertical/smrt-content:ContentContributor');
  const item = await collection.get(params.id);
  if (!item) throw error(404, '@happyvertical/smrt-content:ContentContributor not found');

  await item.delete();
  return json({ success: true });
};
