{
  "purpose": "Hierarchical event management with participant tracking, event series, and infinitely nestable event structures.",
  "readmeSections": [
    "Features"
  ],
  "patterns": [
    {
      "name": "Hierarchical events",
      "description": "Events can be infinitely nested (Game -> Period -> Goal -> Assist).",
      "example": "const game = await eventCollection.create({\n  _meta_type: 'Game',\n  title: 'Championship Final',\n  startDate: new Date()\n});\n\nconst period = await eventCollection.create({\n  _meta_type: 'Period',\n  title: 'First Period',\n  parentId: game.id\n});"
    },
    {
      "name": "Event series",
      "description": "Group related events with recurring patterns.",
      "example": "const series = await seriesCollection.create({\n  title: '2024 NBA Finals',\n  recurrencePattern: 'daily'\n});\n\nconst game1 = await eventCollection.create({\n  title: 'Game 1',\n  seriesId: series.id\n});"
    }
  ],
  "pitfalls": [
    "Events use STI - always specify _meta_type when creating event subtypes",
    "Integrates with smrt-places for locations and smrt-profiles for participants"
  ],
  "exports": [
    "Event",
    "EventCollection",
    "EventSeries",
    "EventParticipant"
  ]
}
