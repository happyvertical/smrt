# =============================================================================
# GENERATED FILE - DO NOT EDIT DIRECTLY
# =============================================================================
# This file is automatically generated by the HappyVertical standardization
# script. Any manual changes will be overwritten on the next update.
#
# Source: https://github.com/happyvertical/sdk/.github/workflows/on-merge-main-template.yml
#
# To update:
#   bun scripts/standardize-repo.ts --repo [owner/repo] --path [path]
# =============================================================================

name: On Merge to Main

on:
  push:
    branches: [main, master]

permissions:
  contents: read
  packages: read

jobs:
  orchestrate:
    uses: happyvertical/sdk/.github/workflows/shared-merge-orchestrator.yml@main
    with:
      repo_owner: ${{ github.repository_owner }}
      repo_name: ${{ github.event.repository.name }}
      ref: ${{ github.ref }}
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
