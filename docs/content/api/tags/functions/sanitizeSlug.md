# Function: sanitizeSlug()

> **sanitizeSlug**(`input`): `string`

Defined in: [tags/src/utils.ts:28](https://github.com/happyvertical/smrt/blob/71a16025d52b026725fd522a392015e67e1d6489/packages/tags/src/utils.ts#L28)

Sanitize slug input

Converts to lowercase, replaces spaces with hyphens,
removes invalid characters, and ensures proper format.

## Parameters

### input

`string`

The input string to sanitize

## Returns

`string`

Sanitized slug
