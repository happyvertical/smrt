{
  "name": "{{PACKAGE_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "init-data": "tsx scripts/init-data.ts",
    "workflow:caelus": "npx @happyvertical/caelus --config smrt.config.js",
    "workflow:praeco": "npx @happyvertical/praeco --config smrt.config.js",
    "validate:json": "tsx scripts/validate-json.ts",
    "validate:slugs": "tsx scripts/validate-slugs.ts"
  },
  "devDependencies": {},
  "dependencies": {}
}
